{
  "name": "privcord",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
